export function initSettingsModal({ modal, closeButton, status, keyManager, proxyUrl }) { status.textContent = proxyUrl ? 'Đang dùng serverless proxy; API key được giữ ở server.' : (keyManager.size ? `${keyManager.size} API key đã được nạp local` : 'Chưa tìm thấy API key.'); closeButton.onclick = () => modal.classList.remove('open'); return () => modal.classList.add('open'); }
