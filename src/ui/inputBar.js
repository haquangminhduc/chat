import { byId } from '../utils/dom.js';
import { toast } from './toast.js';
export function initInputBar({ prompt, sendButton, uploadButton, imageInput, preview, previewImage, previewName, removeImage, micButton, composer, onSend }) { let imagePart = null; const resize = () => { prompt.style.height = 'auto'; prompt.style.height = `${Math.min(prompt.scrollHeight, 200)}px`; byId('charCount').textContent = `${prompt.value.length.toLocaleString()} / 8.000`; }; const submit = () => { const text = prompt.value.trim(); if (text || imagePart) onSend(text, imagePart, clearImage); }; sendButton.onclick = submit; prompt.oninput = resize; prompt.onkeydown = event => { if (event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); submit(); } }; uploadButton.onclick = () => imageInput.click(); imageInput.onchange = () => { const file = imageInput.files[0]; if (!file) return; compressImage(file, result => { imagePart = { name: file.name, mime: 'image/jpeg', data: result.split(',')[1] }; previewImage.src = result; previewName.textContent = `${file.name} · nén`; preview.classList.add('show'); }); }; function compressImage(file, done) { const reader = new FileReader(); reader.onload = () => { const image = new Image(); image.onload = () => { const scale = Math.min(1, 1280 / Math.max(image.width, image.height)); const canvas = document.createElement('canvas'); canvas.width = Math.max(1, Math.round(image.width * scale)); canvas.height = Math.max(1, Math.round(image.height * scale)); canvas.getContext('2d').drawImage(image, 0, 0, canvas.width, canvas.height); done(canvas.toDataURL('image/jpeg', .82)); }; image.src = reader.result; }; reader.readAsDataURL(file); } function clearImage() { imagePart = null; imageInput.value = ''; preview.classList.remove('show'); } removeImage.onclick = clearImage; let recognition; micButton.onclick = () => { const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition; if (!SpeechRecognition) return toast('Trình duyệt không hỗ trợ nhập giọng nói', 'error'); if (recognition) { recognition.stop(); recognition = null; return; } recognition = new SpeechRecognition(); recognition.lang = 'vi-VN'; recognition.interimResults = true; recognition.onresult = event => { prompt.value = [...event.results].map(result => result[0].transcript).join(''); resize(); }; recognition.onend = () => { recognition = null; }; recognition.start(); toast('Đang lắng nghe...'); }; return { setGenerating(value) { composer.classList.toggle('is-generating', value); }, resize, getImage: () => imagePart }; }
