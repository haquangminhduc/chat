import { activeChat, chatStore } from '../state/chatStore.js';
import { renderMessage } from './message.js';
import { renderWelcome } from './welcome.js';
export function createChatArea({ container, onPrompt, actions }) { function render() { const chat = activeChat(); if (!chat || !chat.messages.length) { renderWelcome(container, onPrompt); return; } container.innerHTML = ''; chat.messages.forEach((message, index) => container.append(renderMessage(message, index, actions))); requestAnimationFrame(() => { container.scrollTop = container.scrollHeight; }); } function updateStreaming(message) { const bubble = container.querySelector('.message:last-child .message-bubble'); if (!bubble) return; bubble.innerHTML = message.content ? (window.DOMPurify?.sanitize(window.marked?.parse(message.content) || '') || '') : '<div class="typing"><i></i><i></i><i></i></div>'; bubble.classList.add('streaming'); const nearBottom = container.scrollHeight - container.scrollTop - container.clientHeight < 140; if (nearBottom) container.scrollTop = container.scrollHeight; } const unsubscribe = chatStore.subscribe(render); render(); return { render, updateStreaming, destroy: unsubscribe }; }
