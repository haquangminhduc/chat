export function initParticles(canvas) { const ctx = canvas.getContext('2d'); let dots = []; function resize() { canvas.width = innerWidth; canvas.height = innerHeight; dots = Array.from({ length: Math.min(70, Math.floor(innerWidth / 18)) }, () => ({ x: Math.random() * canvas.width, y: Math.random() * canvas.height, r: Math.random() * 1.8 + .4, vx: (Math.random() - .5) * .25, vy: (Math.random() - .5) * .25, a: Math.random() * .45 + .1 })); } function draw() { ctx.clearRect(0, 0, canvas.width, canvas.height); dots.forEach(dot => { dot.x = (dot.x + dot.vx + canvas.width) % canvas.width; dot.y = (dot.y + dot.vy + canvas.height) % canvas.height; ctx.beginPath(); ctx.arc(dot.x, dot.y, dot.r, 0, Math.PI * 2); ctx.fillStyle = `rgba(167,139,250,${dot.a})`; ctx.fill(); }); requestAnimationFrame(draw); } addEventListener('resize', resize); resize(); draw(); }
